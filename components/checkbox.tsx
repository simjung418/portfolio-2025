import { CheckIcon } from "@heroicons/react/16/solid";

export default function () {
  return (
    <section className={`flex shrink-0 justify-center items-center size-7 box-border rounded-lg p-0.5`}>
      <CheckIcon className="font-bold" />
    </section>
  );
}
